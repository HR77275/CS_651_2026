# 3DHGS / Half-Gaussian Splatting dependencies.
# Recommended setup:
#   conda env create --file 3DHGS/environment.yml
#   conda activate half_gaussian_splatting
#
# 3DHGS is tied to Python 3.7 + PyTorch 1.12.1 + CUDA 11.6 custom extensions.
# Do not install this into the modern segmentation environment from requirements.txt.
#
# After cloning/restoring the full upstream 3DHGS source with submodules, install the
# CUDA extensions from inside the 3DHGS directory:
#   pip install ./submodules/diff-gaussian-rasterization
#   pip install ./submodules/simple-knn

numpy<1.22
matplotlib<3.6
tqdm
plyfile
lpips==0.1.4
scipy==1.7.3